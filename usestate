import { useState } from "react";

function UseStateExample(){
 const [data, setData] = useState(0);
    function handler(){
        setData(data+1);
    }
    return( 
        <>
            <h1>{data}</h1>
            <button onClick = {()=> handler()}>push</button>
        </>
    )
}
export default UseStateExample;
